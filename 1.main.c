#include <stdio.h>                     // Loading the libraby
int main(int argc, char const *argv[]) // main is a container or function under which the c statements are written which returns value as a integer
{
    printf("Hello World"); // semicolon is must after each keyword
    return 0;
}
